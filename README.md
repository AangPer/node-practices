# Notas:

> Este es un **ejercicio** de Node.

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Toma la base de la tabla de multiplicar    [número] [requerido]
  -l, --listar   Muestra la tabla creada             [booleano] [defecto: false]
  -n, --limite   Define hasta que numero se multiplicara la base        [número]
```
